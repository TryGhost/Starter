{{!< default}}
<div class="gh-page">
    <div class="gh-container">

        <div class="gh-postfeed">
        {{#foreach posts}}

            {{> "card"}} {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

        {{!-- Outputs pagination links, if available --}}
        <div class="gh-pagination">
            {{pagination}}
        </div>
    </div>
</div>
